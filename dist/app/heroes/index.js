/*
@todo: build barrel, exporting everything required in the module, so
    when one need to import, only need:
    import { HeroesComponent } from './heroes
*/
// export * from './heroes.component'; 
